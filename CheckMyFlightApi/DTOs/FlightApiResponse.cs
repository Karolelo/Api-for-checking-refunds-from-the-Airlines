namespace CheckMyFlightApi.DTOs;
/// <summary>
/// For future development I would extend it a little
/// for now it's simple, but we could add
/// some array or list with possible errors
/// and something like bool Succes
/// </summary>
public class FlightApiResponse 
{
    public string Response { get; set; }
}